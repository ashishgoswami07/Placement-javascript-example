const person = [{
        name: "Tony",
        age: 5,
    },
    {
        name: "star",
        age: 3,
    }
];

// person.forEach(p => console.log(p));

const v = function abc(p) {
    // console.log("hi");
    console.log(p);
};
person.forEach(v);